# <<< aliases
alias +='pushd .'
alias -- -='popd'
alias ..='cd ..'
alias ...='cd ../..'
alias dir='ls -l'
alias l='ls -alF'
alias la='ls -la'
alias ll='ls -l'
alias ls-l='ls -l'
alias md='mkdir -p'
alias ack='ack-grep'

# ssh

# sshfs

# utilities
alias gv='gvim $* 2> /dev/null'
